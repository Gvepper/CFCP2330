/** Initializes a new game and starts running it. */
class Main {
    function void main() {
        var JumperGame game;
        let game = JumperGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
